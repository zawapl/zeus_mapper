[package]
name = "zeus_mapper"
version = "0.1.1"
authors = ["Jakub Zawadzki <kuba.zaw@gmail.com>"]
edition = "2024"
license = "MIT"

[lints]
workspace = true

[dependencies]
explode = "0.1.2"
# pklib = "0.2.0" - has issue with compressing the data, using forked version with a fix for now
pklib = { git = "https://github.com/zawapl/pklib-rs", branch = "issue-2" }
encoding_rs = "0.8.35"
my_macros = { path = "../my_macros" }
regex = "1.12.2"

[dev-dependencies]
image = "0.25.2"